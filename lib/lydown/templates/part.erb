<%
  title = nil
  id = 'Staff'
  if name && (name != '') && !self['mode'] == :part
    title = Lydown::Rendering.part_title(name)
    id = "#{name.gsub(' ', '')}Staff"
  end
%>

\new Staff = <%= id %> \with {
}

\context Staff = <%= id %> {
<% if title %>\set Staff.instrumentName = #"<%= title %>"<% end %>
  \relative c { <%= part['music'] %> }
}

