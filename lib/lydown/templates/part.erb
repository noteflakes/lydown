<%
  title = nil
  if name && (name != '')
    title = Lydown::Rendering.part_title(name)
  end
  id = "#{title && title.gsub(' ', '')}Staff"
  
  score_mode = self['render_opts/mode'] == :score
%>

\new Staff = <%= id %> \with {
}

\context Staff = <%= id %> {
<% if score_mode %>\set Staff.instrumentName = #"<%= title %>"<% end %>
  \relative c { <%= part['music'] %> }
}

